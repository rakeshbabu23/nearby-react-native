import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PostsVideos = () => {
  return (
    <View style={styles.container}>
      <Text>PostsVideos</Text>
    </View>
  );
};

export default PostsVideos;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "red",
  },
});
